// Vite resolves an imported asset to its served URL string at build time.
// The Wu "W" is the real (user-provided) gold logo on transparent. Used as a noncommercial fan
// tribute — see the disclaimer in copy.js / README; the "W" is a Wu-Tang trademark, not PD.
import wuMark from './assets/Wu-Tang-Logo.png';

export const assets = { wuMark };
